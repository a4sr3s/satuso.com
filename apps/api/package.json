{
  "name": "@satuso/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "lint": "tsc --noEmit",
    "db:generate": "wrangler d1 migrations create crm-db",
    "db:migrate:local": "wrangler d1 migrations apply crm-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply crm-db --remote"
  },
  "dependencies": {
    "@clerk/backend": "^2.29.4",
    "@hono/zod-validator": "^0.7.6",
    "@tsndr/cloudflare-worker-jwt": "^2.5.0",
    "groq-sdk": "^0.37.0",
    "hono": "^4.4.0",
    "nanoid": "^5.0.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "@satuso/shared": "*",
    "typescript": "^5.3.0",
    "wrangler": "^3.91.0"
  }
}
