---
import { Icon } from "astro-icon/components";
import { SITE } from "../config/site.mjs";
---

<header
  class="w-full py-4 px-4 border-b border-gray-200 bg-white sticky top-0 z-50"
  role="banner"
>
  <nav
    class="max-w-6xl mx-auto flex items-center justify-between"
    aria-label="Main navigation"
  >
    <a
      href="/"
      aria-label="Satuso - Home"
    >
      <img src="/logo.svg" alt="Satuso" class="h-10" />
    </a>

    <!-- Mobile menu button -->
    <button
      type="button"
      class="md:hidden p-2 text-gray-600 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-black focus:ring-offset-2 rounded-lg"
      aria-label="Toggle menu"
      aria-expanded="false"
      aria-controls="mobile-menu"
      id="mobile-menu-button"
    >
      <Icon name="tabler:menu-2" class="w-6 h-6" aria-hidden="true" />
    </button>

    <!-- Desktop navigation -->
    <ul class="hidden md:flex items-center gap-1" role="list">
      <li>
        <a
          href="/#features"
          class="px-4 py-2 text-gray-600 rounded-lg hover:bg-gray-100 hover:text-gray-900 transition-colors text-sm"
        >
          Features
        </a>
      </li>
      <li>
        <a
          href="/#pricing"
          class="px-4 py-2 text-gray-600 rounded-lg hover:bg-gray-100 hover:text-gray-900 transition-colors text-sm"
        >
          Pricing
        </a>
      </li>
      <li>
        <a
          href="/about"
          class="px-4 py-2 text-gray-600 rounded-lg hover:bg-gray-100 hover:text-gray-900 transition-colors text-sm"
        >
          About
        </a>
      </li>
      <li>
        <a
          href="/contact-sales"
          class="px-4 py-2 text-gray-600 rounded-lg hover:bg-gray-100 hover:text-gray-900 transition-colors text-sm"
        >
          Contact Sales
        </a>
      </li>
      <li class="ml-2">
        <a
          href={`${SITE.appUrl}/login`}
          class="px-4 py-2 bg-black text-white rounded-lg hover:bg-gray-800 transition-colors text-sm"
        >
          Get Started
        </a>
      </li>
    </ul>
  </nav>

  <!-- Mobile menu -->
  <div
    id="mobile-menu"
    class="hidden md:hidden border-t border-gray-200 bg-white"
    role="menu"
    aria-labelledby="mobile-menu-button"
  >
    <ul class="flex flex-col py-4" role="list">
      <li class="px-4 py-2">
        <a
          href="/#features"
          class="block w-full text-center px-4 py-2 text-gray-600 rounded-lg hover:bg-gray-100 hover:text-gray-900 transition-colors"
          role="menuitem"
        >
          Features
        </a>
      </li>
      <li class="px-4 py-2">
        <a
          href="/#pricing"
          class="block w-full text-center px-4 py-2 text-gray-600 rounded-lg hover:bg-gray-100 hover:text-gray-900 transition-colors"
          role="menuitem"
        >
          Pricing
        </a>
      </li>
      <li class="px-4 py-2">
        <a
          href="/about"
          class="block w-full text-center px-4 py-2 text-gray-600 rounded-lg hover:bg-gray-100 hover:text-gray-900 transition-colors"
          role="menuitem"
        >
          About
        </a>
      </li>
      <li class="px-4 py-2">
        <a
          href="/contact-sales"
          class="block w-full text-center px-4 py-2 text-gray-600 rounded-lg hover:bg-gray-100 hover:text-gray-900 transition-colors"
          role="menuitem"
        >
          Contact Sales
        </a>
      </li>
      <li class="px-4 py-2">
        <a
          href={`${SITE.appUrl}/login`}
          class="block w-full text-center px-4 py-2 bg-black text-white rounded-lg hover:bg-gray-800 transition-colors"
          role="menuitem"
        >
          Get Started
        </a>
      </li>
    </ul>
  </div>
</header>

<script>
  // Mobile menu toggle functionality
  const menuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');

  if (menuButton && mobileMenu) {
    menuButton.addEventListener('click', () => {
      const isExpanded = menuButton.getAttribute('aria-expanded') === 'true';
      menuButton.setAttribute('aria-expanded', String(!isExpanded));
      mobileMenu.classList.toggle('hidden');
    });

    // Close menu when clicking outside
    document.addEventListener('click', (e) => {
      if (
        !menuButton.contains(e.target as Node) &&
        !mobileMenu.contains(e.target as Node) &&
        !mobileMenu.classList.contains('hidden')
      ) {
        menuButton.setAttribute('aria-expanded', 'false');
        mobileMenu.classList.add('hidden');
      }
    });

    // Close menu on escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && !mobileMenu.classList.contains('hidden')) {
        menuButton.setAttribute('aria-expanded', 'false');
        mobileMenu.classList.add('hidden');
        menuButton.focus();
      }
    });
  }
</script>
